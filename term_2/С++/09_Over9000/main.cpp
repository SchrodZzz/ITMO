#include "lib/Quiz.h"
#include "lib/Random.h"

//TODO: ask about using map for switching instead of switch-case (for parser purposes)

//TODO: (just for me) debugging without exceptions is freaking hard, so next time, before coding - read about them (-:

int main()
{
    Quiz quiz;
    quiz.start();
    return 0;
}